<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disable background -->
    <item android:state_enabled="false"
        android:color="@color/color_imgbtn_drink_disabled"
        android:drawable="@drawable/ic_drink_disabled"/>

    <!-- Enabled background -->
    <item android:state_enabled="true"
        android:color="@color/color_imgbtn_drink_enabled"
        android:drawable="@drawable/ic_drink_enabled"/>

</selector>